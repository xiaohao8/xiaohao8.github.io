[build]
  publish = "public"
  command = "npm install hexo-renderer-pug hexo-renderer-stylus --save;npm install hexo-generator-feed --save;npm i hexo-url-submission;npm run build"

[build.environment]
  NODE_VERSION = "14"
