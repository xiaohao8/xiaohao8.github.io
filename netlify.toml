[build]
  publish = "public"
  command = "npm install hexo-generator-feed --save;npm install --save hexo-helper-live2d;npm install --save hexo-blog-encrypt;npm run build"

[build.environment]
  NODE_VERSION = "14"
